<resources>

    <!-- Style pour la team 1 -->
    <style name="Team1" parent="AppBaseTheme">
        <item name="android:textColor">@color/red</item>
    </style>
    <!-- Style pour la team 2 -->

    <style name="Team2" parent="AppBaseTheme">
        <item name="android:textColor">@color/blue</item>
    </style>

    <!-- Style pour la desactivation -->
    <style name="TeamNone" parent="AppBaseTheme">
        <item name="android:textColor">@color/gray</item>
    </style>

    <!-- Style pour l'activation -->
    <style name="TeamAll" parent="AppBaseTheme">
        <item name="android:textColor">@color/white</item>
    </style>
    <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    <style name="ratingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/custom_ratingbar</item>
        <item name="android:minHeight">16dip</item>
        <item name="android:maxHeight">16dip</item>
    </style>

    <!-- AUTOGENERATED -->
    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.Sherlock">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme"></style>

</resources>