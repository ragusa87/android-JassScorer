<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- if you move this, update MainActivity.MENU_CANCEL_POSITION -->
    <item
        android:showAsAction="ifRoom|withText"
        android:id="@+id/menu_cancel"
        android:icon="@drawable/ic_menu_content_undo"
        android:title="@string/menu_cancel"/>
    <item
        android:showAsAction="ifRoom|withText"
        android:id="@+id/menu_reset"
        android:icon="@drawable/ic_menu_content_discard"
        android:title="@string/menu_reset"/>
    <item
        android:showAsAction="ifRoom|withText"
        android:icon="@drawable/ic_menu_action_settings"
        android:id="@+id/menu_settings"
        android:title="@string/menu_settings"/>

</menu>